# Fluid theme overrides.
fun_features:
  typing:
    startDelay: 500
    typeSpeed: 85
    backDelay: 3000
    backSpeed: 50
    cursorChar: "_"
    loop: true

post:
  mermaid:
    enable: true
    specific: true
    options:
      theme: default
